<h2>Sign In</h2>

<%= form_tag("") do %>		

<!-- error messages to be put here -->

	<div class="form-group">
		<label>Email: </label>
		<%= text_field_tag(:email, "")%>
	</div>
		<div class="form-group">
		<label>Password: </label>
		<%= password_field_tag(:password) %>
	</div>
	
	<%= submit_tag("SignIn") %>
<% end %>